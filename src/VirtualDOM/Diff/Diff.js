"use strict"
import {
  TEXT,
  PROPS,
  REORDER,
  REPLACE
} from './PatchType'
import ListDiff from './ListDiff'

export const Diff = (
  prevNode,
  nextNode,
  index = 0,
  patches = {}
) => {
  const currentPatches = []

}
